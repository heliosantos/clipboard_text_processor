# -*- coding: utf-8 -*-

from setuptools import setup, find_packages

setup(
    name='clipboard_text_processor',
    version='1.0',
    description='clipboard_text_processor',
    packages=find_packages()
)
